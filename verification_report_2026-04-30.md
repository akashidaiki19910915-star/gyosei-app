# 事前検証レポート（2026-04-30）

## 前提
- 本検証は**修正なし**（新機能追加・DB変更・UI変更・既存ロジック変更・不具合修正を実施しない）で実施。
- この環境では Supabase の有効なログイン資格情報およびブラウザ手動操作セッションがないため、29項目の業務シナリオを実データで最後まで通す E2E 実行は不可。
- そのため、`app.js` / `index.html` の静的確認と、最小限の構文チェックのみ実施。

## 実施結果（総括）
OK：
- JavaScript 構文チェックは成功（`node --check app.js`）。
- `data-action` の未登録時に `console.error("未登録data-action", ...)` を出す防御ロジックを確認。
- 各主要テーブル読込で `.eq("user_id", currentUser.id)` が使われ、RLS前提の実装方針を確認。

NG：
- 29項目の業務フロー（登録→編集→削除、帳票出力、一覧からの各種出力、連続操作、タブ移動後操作、Console赤エラー有無など）は、認証付きブラウザE2E未実施のため**合否判定不可**。
- 重点確認項目（403/42501、400カラム不一致、金額整合、ファイル保存可否、LOADING FORCE TIMEOUT 実発生有無）は、実行ログ未取得のため**再現確認不可**。

再現手順：
1. Supabase URLを有効化した公開URL（https配信）で起動する。
2. テストユーザーでログインする。
3. 検証対象1〜29を順番通りに実施する。
4. 各操作直後にDevTools Console / Networkを確認する。
5. 失敗時はステータスコード（403/400等）、APIレスポンス、操作直前の入力値を記録する。

Consoleエラー：
- 本環境ではブラウザ実行を行っていないため、実コンソール赤エラーは未採取。
- コード上は以下の監視対象ログ出力を確認。
  - `未登録data-action`
  - `click action failed`
  - `LOAD <table> ERROR`

原因分類：
- A. RLS / user_id：**要実機確認**（コード上は user_id フィルタあり）
- B. DBカラム不一致：**要実機確認**（READMEに追加SQL前提が複数あり、未適用時に400系リスク）
- C. data-action未登録：**低リスク（検知ロジックあり）**
- D. 金額計算：**要実機確認**（見積/請求/入金/残額の突合未実施）
- E. 帳票出力：**要実機確認**（保存可否未実施）
- F. loading制御：**要実機確認**（実運用データ量でのタイムアウト検証未実施）
- G. render表示：**要実機確認**（タブ移動・更新なし連続操作の画面検証未実施）

修正候補：
- 今回は修正禁止のため未提案（検証時ログ採取の標準化のみ推奨）。

修正優先順位：
- 今回は修正禁止のため未設定。
